[ ТОЧКА ВХОДА: Внешний Хаос ]
            │
            ▼
┌───────────────────────────────────────────────────────────────────────────────────┐
│ ЦЕХ №1. ХРАНИЛИЩЕ СЫРЬЯ (Жесткий Диск Компьютера / Сервер)                       │
│ Физические файлы на диске, присланные субподрядчиками, мастерами или ИТ-системами. │
└───────────────────────────────────────────────────────────────────────────────────┘
            │
            │  ◄─── [ВЕТВЛЕНИЕ №1: Формат упаковки данных]
            │       ├── Если Excel (.xlsx) ➔ Вызываем кран `pd.read_excel()`
            │       ├── Если Текст (.csv)  ➔ Вызываем кран `pd.read_csv()`
            │       └── Если Словарь (.json)➔ Вызываем кран `pd.read_json()`
            │
            ▼  [ТРУБА №1: Загрузка в память]
┌───────────────────────────────────────────────────────────────────────────────────┐
│ ЦЕХ №2. ЛАБОРАТОРИЯ ЗАЧИСТКИ И ТРАНСФОРМАЦИИ (Python + Pandas в RAM)              │
│ Свободная песочница. Данные висят «в воздухе» оперативной памяти. Сверхскоростная │
│ калибровка, выжигание текстового мусора и расчёт формул.                          │
└───────────────────────────────────────────────────────────────────────────────────┘
            │
            │  ◄─── [ВЕТВЛЕНИЕ №2: Стратегия архитектуры (Где чистить данные?)]
            │       │
            │       ├── СТРАТЕГИЯ ETL (Наш домашний контур):
            │       │   Чистим данные ПИТОНОМ прямо в RAM (`.str.strip()`, `.fillna()`),
            │       │   получаем стерильный слиток и толкаем дальше.
            │       │
            │       └── СТРАТЕГИЯ ELT (Твой рабочий контур):
            │           Толкаем данные в базу грязными, как они есть, а чистить будем 
            │           позже внутри СУБД с помощью SQL-запросов и блоков WHERE.
            │
            ▼  [ТРУБА №2: Промышленный Драйвер-Переводчик `psycopg2`]
┌───────────────────────────────────────────────────────────────────────────────────┐
│ СВЯЗУЮЩИЙ МОСТ: Кабель коннекта `create_engine` (SQLAlchemy)                      │
│ Виртуальная труба, пробитая из Питона к СУБД. Внутри шлюза скобок `.to_sql()`     │
│ прописываются 4 жестких параметра отгрузки. Драйвер переводит таблицы в байты.    │
└───────────────────────────────────────────────────────────────────────────────────┘
            │
            ▼  [ВХОД ЧЕРЕЗ ПОРТ БЕЗОПАСНОСТИ: Например, 5433]
┌───────────────────────────────────────────────────────────────────────────────────┐
│ ЦЕХ №3. ВЕЧНЫЙ БРОНИРОВАННЫЙ СКЛАД — СУБД (PostgreSQL / На диске)                 │
│ Невидимый робот-кладовщик. Хранит данные в спрессованных бинарных блоках в папке  │
│ `data`. Защищает информацию от сгорания, выдает её тысячам пользователей.          │
└───────────────────────────────────────────────────────────────────────────────────┘
            │
            │  ◄─── [ВЕТВЛЕНИЕ №3: Защита от разрушения структуры витрин]
            │       ├── Если данные чистились в Питоне (ETL) ➔ Укладываем в чистую TABLE.
            │       └── Если данные грязные (ELT) ➔ Пишем SQL-запрос зачистки и 
            │           запечатываем его в виртуальное стекло — Представление `CREATE VIEW`.
            │
            ▼  [ТРУБА №3: Сетевой коннектор Live Data Connection]
┌───────────────────────────────────────────────────────────────────────────────────┐
│ ЦЕХ №4. АНАЛИТИЧЕСКАЯ ВИТРИНА МАГАЗИНА (BI-Слой: Power BI / Yandex DataLens)     │
│ Графический пульт управления. Никакого кода. Мышкой затягиваем на холст таблицы   │
│ или представления VIEW из СУБД. Рисуем KPI-карточки и графики для директоров.     │
└───────────────────────────────────────────────────────────────────────────────────┘
            │
            ▼
[ ТОЧКА ВЫХОДА: Бизнес-Решение Директора (Финальный SELECT прибыли) ]
