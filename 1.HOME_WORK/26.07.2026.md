# ОТЧЕТ «ПАНДА»: ЦЕХ МНОГОФАКТОРНОЙ ФИЛЬТРАЦИИ И СВОДНОЙ АГРЕГАЦИИ (ДЕНЬ 7)

**Дата фиксации:** 26.07.2026  
**Контур:** Оперативная память (RAM)  
**Целевая задача:** Переход от простых срезов к сложным двухфакторным маскам и группировке данных (аналог блока `GROUP BY` в SQL). Разбор физики широкого и точечного схлопывания матриц в RAM [1.1].

---

## ПРЕДЫСТОРИЯ КОДА И ИСХОДНЫЙ СТЕНД

До начала текущей сессии в оперативной памяти (RAM) был развернут и полностью стерилизован DataFrame с именем `df_excel`, содержащий 4 строки и 6 колонок (включая расчетные дельты и штрафы).
```python
import pandas as pd
df_excel = pd.read_excel("D:/Portable_Data_Suite/data/vagon_data.xlsx")
df_excel["cargo"] = df_excel["cargo"].str.lower().str.strip()
df_excel["status"] = df_excel["status"].str.lower().str.strip()
df_excel["status"] = df_excel["status"].fillna("не указан")
df_excel["number"] = df_excel["number"].fillna(0)
df_excel["number"] = df_excel["number"].astype(int)
df_excel["fact_hours"]=[14,12,8,24]
df_excel["delta_h"]=df_excel["fact_hours"]-12
df_excel["penality_rub"]=df_excel["fact_hours"]*500
df_excel=df_excel.sort_values(by="penality_rub",ascending=False)
df_excel.head()
```

### Исходная матрица `df_excel` на верстаке:
<img width="573" height="164" alt="image" src="https://github.com/user-attachments/assets/9aba6dc5-260b-41a3-9546-fe6d5dc0a560" />

---

## ЭПИЗОД I: СЛОЖНАЯ МНОГОФАКТОРНАЯ ФИЛЬТРАЦИЯ (WHERE)

Бизнес-задача на производстве часто требует пересечения условий. Нам потребовалось изолировать только те вагоны, которые генерируют простой **И** при этом их штраф уже превысил порог в 5000 рублей [INDEX].

### Шаг 1. Сборка составного клише через Амперсанд

```python
double_mask = (df_excel["status"] == "простой") & (df_excel["penality_rub"] > 5000)
df_excel[double_mask].head()
```

#### Анатомический разбор роли каждого символа:
*   **`double_mask =`** — Переменная-посредник (Стиль Контура №2). Мы упаковываем сложную формулу в одно короткое имя, чтобы код оставался читаемым и не превращался в многоэтажную нечитаемую матрешку [INDEX].
*   **`(...) & (...)`** — Логическое **И** (оператор Амперсанд). Приказывает системе: «Выживут только те строки, где оба условия вернули True одновременно» [INDEX].
*   **`круглые скобки (...)`** — Жесткое требование синтаксиса Pandas [INDEX]. Они изолируют каждое отдельное условие, страхуя конвейер от аварии приоритета математических операций [INDEX].
*   **`df_excel[double_mask]`** — Наш базовый внешний шлюз-приемник квадратных скобок [INDEX]. Он принимает готовую карту `True/False` из переменной `double_mask`, накладывает её на таблицу и выводит временный виртуальный срез данных [INDEX].
*   * СОСТОЯНИЕ ИСХОДНИКА:** С самой таблицей `df_excel` в RAM ничего не произошло [INDEX]. Она сохранила все 4 строки [INDEX]. Мы лишь вывели её временное зеркало на экран, так как не использовали знак `=` для перезаписи исходника [INDEX].

#### Вывод на экране после фильтрации:

<img width="535" height="102" alt="image" src="https://github.com/user-attachments/assets/9773fdd6-9c5d-4d3e-a493-3e72d3bcad7c" />

*Вагон `3556` был отсечен вектором, так как его статус в RAM равен `"не указан"`, что нарушило логику оператора `&` [INDEX].*

---

## 📊 ЭПИЗОД II: СВОДНАЯ АГРЕГАЦИЯ ДАННЫХ (GROUP BY)

Бизнес-задача: Схлопнуть построчный хаос и калькулировать суммарные показатели простоя и убытков отдельно по каждой категории груза (`cargo`) [1.1].

### Вариант №1. Широкое табличное схлопывание всех колонок

```python
df_grouped = df_excel.groupby("cargo").sum(numeric_only=True)
df_grouped.head()
```

#### Анатомический разбор роли каждого слова:
*   **`.groupby("cargo")`** — Промышленный инструмент нарезки [1.1]. Он находит в колонке `"cargo"` уникальные значения (`грунт` и `зерно`) и делит таблицу в RAM на изолированные кусты по этим категориям [1.1].
*   **`.sum(...)`** — Математическая функция схлопывания [1.1]. Она суммирует показатели внутри получившихся кустов [1.1].
*   **`numeric_only=True`** — Жесткий системный флаг безопасности [1.1]. Приказ Pandas: «Считай сумму только в цифровых столбцах, игнорируй текстовые буквы статусов» [1.1]. Страхует скрипт от падения [1.1].

#### Результат в RAM (DataFrame):

<img width="417" height="128" alt="image" src="https://github.com/user-attachments/assets/533a76ef-4255-4241-9ca4-02f4e4a188cb" />

*   **Физика структуры:** Колонка `cargo` превратилась в **системный индекс таблицы (Index)** взамен старых цифр строк [1.1]. На выходе мы получили широкую двухмерную таблицу, где просчитаны суммы по всем числовым столбцам одновременно [1.1].

---

### Вариант №2. Точечное снайперское вырезание одной метрики

```python
df_ton_sum = df_excel.groupby("cargo")["penality_rub"].sum()
df_ton_sum
```

#### Анатомический разбор матрешки со шлюзом:
*   **`df_excel.groupby("cargo")`** — Система так же нарезает исходный DataFrame на кусты грунта и зерна [1.1].
*   **`["penality_rub"]`** — **Внутренний точечный шлюз квадратных скобок** [1.1]. Он врезается вглубь сгруппированных кустов и принудительно вырезает из них *только одну* колонку штрафов, немедленно выбрасывая из оперативной памяти все остальные числовые столбцы (`number`, `fact_hours`) [1.1]. Защищает RAM от перегрузки при работе с Big Data [1.1].
*   **`.sum()`** — Функция калькулирует сумму только над вырезанным вектором штрафов [1.1].

#### Результат в RAM (Одномерный Series):
<img width="288" height="106" alt="image" src="https://github.com/user-attachments/assets/010e610e-108b-4fad-9fa1-87ca99d6e5d9" />
*   **Физика структуры:** Широкие столбцы исчезли [1.1]. На верстак выгружена стерильная, лаконичная именованная числовая лента данных класса `Series`, готовая к точечной отправке в финансовый отчет [1.1].

---

###  Сравнительный паспорт цеха агрегации

| Конструкция в коде | Что происходит внутри RAM? | Что получаем на выходе? |
| :--- | :--- | :--- |
| **`df.groupby("A").sum(numeric_only=True)`** | Группирует по колонке `A` и обсчитывает сумму **всех числовых столбцов** таблицы разом [1.1]. | Широкий, многоколоночный **DataFrame** [1.1]. |
| **`df.groupby("A")["B"].sum()`** | Группирует по `A`, шлюзом `["B"]` вырезает **строго один столбец `B`**, уничтожая остальные из расчетов [1.1]. | Одномерная компактная лента **Series** [1.1]. |

